#include <stdio.h>
#include "lib/game.c"

void run_test() {
    /* Run all needed tests of the new features */
}

int main() {
    run_test();
    printf("Everything works... press ENTER to quit\n");
    getchar();
    return 0;
}
